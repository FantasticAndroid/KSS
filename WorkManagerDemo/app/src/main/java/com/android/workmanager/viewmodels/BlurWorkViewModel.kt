package com.android.workmanager.viewmodels

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class BlurWorkViewModel(application: Application) : AndroidViewModel(application) {
}